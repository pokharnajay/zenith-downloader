# Youtube Downloader Environment Variables

# ========================================
# REQUIRED VARIABLES
# ========================================

# Admin Password (for cookie management at /admin)
# SECURITY: Use a strong password! This protects cookie uploads.
ADMIN_PASSWORD=change_this_to_a_strong_password

# ========================================
# OPTIONAL VARIABLES
# ========================================

# Node Environment (automatically set by Docker, but you can override)
# NODE_ENV=production

# Next.js Port (defaults to 3000, do not change unless you know what you're doing)
# PORT=3000

# Next.js Hostname (defaults to 0.0.0.0 for Docker)
# HOSTNAME=0.0.0.0

# ========================================
# COOKIE ROTATION SETTINGS
# ========================================

# Cookie rotation strategy: round-robin, least-used, or health-based
# COOKIE_ROTATION_STRATEGY=round-robin

# Number of failures before rotating to next cookie (default: 3)
# COOKIE_FAILURE_THRESHOLD=3

# Health check interval in milliseconds (default: 3600000 = 60 minutes)
# HEALTH_CHECK_INTERVAL=3600000

# ========================================
# CHROMIUM FALLBACK SETTINGS
# ========================================

# Enable Chromium fallback when all cookies are blocked (default: true)
# CHROMIUM_FALLBACK_ENABLED=true

# Minimum wait time in ms before extracting cookies (default: 700)
# CHROMIUM_WARMUP_MIN_WAIT=700

# Maximum wait time in ms before extracting cookies (default: 2000)
# CHROMIUM_WARMUP_MAX_WAIT=2000

# ========================================
# NOTES
# ========================================
# 1. Copy this file to `.env` and fill in your actual values
# 2. Never commit `.env` to version control (it's in .gitignore)
# 3. ADMIN_PASSWORD is REQUIRED for cookie management security
# 4. Access admin panel at: http://your-domain/admin
